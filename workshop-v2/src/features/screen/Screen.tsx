import { Shell } from '../../app/Shell';

export function Screen() {
  return (
    <Shell title="Workshop screen">
      <p class="muted center">The wall board arrives with stage 6.</p>
    </Shell>
  );
}
