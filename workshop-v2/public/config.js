/* RCK Workshop v2 — site configuration.
   Supabase → Settings → API. The anon key is safe to publish here: every
   table is behind row-level security, and nothing can be read or written
   without signing in. Never put the service_role key anywhere near this. */
window.RCKW2_CONFIG = {
  supabaseUrl: '',
  supabaseKey: ''
};
